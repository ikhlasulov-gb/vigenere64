# vigenere64
Browser-based tool implementing a custom Vigenère cipher (ROT1, key "islam") with deterministic preprocessing rules, plus a Base64 encoder. Vanilla JS, no dependencies, runs entirely client-side.
